[defaults]
inventory         = inventory/hosts.ini
remote_user       = root
host_key_checking = False
forks             = 10
timeout           = 30
stdout_callback   = yaml       # salida más legible en clase
deprecation_warnings = False

[privilege_escalation]
become       = False           # ya somos root en los nodos
become_method = sudo
